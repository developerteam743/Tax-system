import React, { useState } from 'react';
import type { StockItem } from '../types/tax';
import { formatCurrency } from '../utils/gst';
import { Package, AlertTriangle, Plus, X, CheckCircle2 } from 'lucide-react';
interface StockRegisterProps { stockItems: StockItem[]; onAddStockItem?: (newItem: StockItem) => void; }
export const StockRegister: React.FC<StockRegisterProps> = ({ stockItems, onAddStockItem }) => {
  const [showAddModal, setShowAddModal] = useState(false); const [name, setName] = useState(''); const [hsn, setHsn] = useState(''); const [category, setCategory] = useState('Computer Peripherals'); const [currentStock, setCurrentStock] = useState(10); const [purchasePrice, setPurchasePrice] = useState(1000); const [sellingPrice, setSellingPrice] = useState(1500); const [gstRate, setGstRate] = useState(18);
  const totalStockValue = stockItems.reduce((acc, st) => acc + st.currentStock * st.purchasePrice, 0); const lowStockCount = stockItems.filter(st => st.currentStock <= st.minStockLevel).length;
  const handleSaveItem = () => { if (!name.trim()) return; onAddStockItem?.({ id: `st-${Date.now()}`, name, hsn: hsn || '84716060', category, unit: 'PCS', currentStock, minStockLevel: 5, purchasePrice, sellingPrice, gstRate, lastUpdated: new Date().toISOString().split('T')[0] }); setShowAddModal(false); setName(''); setHsn(''); };
  return <div className="space-y-6 animate-fadeIn">
    <div className="flex flex-col lg:flex-row lg:items-center justify-between gap-4 bg-white p-4 sm:p-6 rounded-2xl border border-slate-200 shadow-md"><div className="min-w-0"><h2 className="text-lg sm:text-xl font-bold text-slate-900 flex items-start gap-2"><Package className="w-6 h-6 text-amber-600 shrink-0" /> <span>Stock Register &amp; Inventory Master</span></h2><p className="text-xs text-slate-500 mt-1">Auto-inward from AI Purchase OCR &amp; auto-outward from sales invoices.</p></div><div className="flex flex-wrap items-center gap-2"><div className="bg-emerald-50 px-3 py-2 rounded-xl"><span className="text-[10px] text-slate-400 block">Stock valuation</span><span className="text-base font-black text-emerald-600">{formatCurrency(totalStockValue)}</span></div>{lowStockCount > 0 && <div className="bg-amber-50 border border-amber-200 px-3 py-2 rounded-xl text-amber-700 text-xs font-bold flex items-center gap-1.5"><AlertTriangle className="w-4 h-4" /> {lowStockCount} Low Stock</div>}<button onClick={() => setShowAddModal(true)} className="w-full sm:w-auto min-h-11 flex items-center justify-center gap-2 text-xs font-bold px-4 py-2.5 rounded-xl bg-gradient-to-r from-blue-600 to-indigo-600 text-white shadow-md"><Plus className="w-4 h-4" /> Add New SKU Item</button></div></div>
    <div className="bg-white border border-slate-200 rounded-2xl shadow-md overflow-hidden"><div className="p-4 border-b border-slate-100 text-xs font-bold text-slate-800">Item Master Register ({stockItems.length} SKUs)</div><div className="hidden md:block overflow-x-auto"><table className="w-full text-left text-xs text-slate-700"><thead className="bg-slate-50 text-slate-500 uppercase text-[10px] tracking-wider font-bold"><tr><th className="p-3">Item Name</th><th className="p-3">HSN</th><th className="p-3">Category</th><th className="p-3">Stock</th><th className="p-3">Buying</th><th className="p-3">Selling</th><th className="p-3">GST</th><th className="p-3 text-right">Valuation</th></tr></thead><tbody className="divide-y divide-slate-100">{stockItems.map(st => { const isLow=st.currentStock<=st.minStockLevel; return <tr key={st.id}><td className="p-3 font-bold">{st.name}{isLow&&<span className="ml-2 text-[9px] px-2 py-0.5 rounded bg-amber-100 text-amber-700">LOW STOCK</span>}</td><td className="p-3 font-mono">{st.hsn}</td><td className="p-3">{st.category}</td><td className={`p-3 font-black ${isLow?'text-amber-600':'text-emerald-600'}`}>{st.currentStock} {st.unit}</td><td className="p-3">{formatCurrency(st.purchasePrice)}</td><td className="p-3 font-bold">{formatCurrency(st.sellingPrice)}</td><td className="p-3 font-mono text-blue-600 font-bold">{st.gstRate}%</td><td className="p-3 text-right font-black">{formatCurrency(st.currentStock*st.purchasePrice)}</td></tr>})}</tbody></table></div><div className="md:hidden divide-y divide-slate-100">{stockItems.map(st => { const isLow=st.currentStock<=st.minStockLevel; return <article key={st.id} className="p-4 space-y-3"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="font-bold text-sm break-words">{st.name}</div><div className="text-[11px] text-slate-500 font-mono mt-1">HSN {st.hsn} · {st.category}</div></div><span className={`shrink-0 px-2 py-1 rounded-lg text-[10px] font-bold ${isLow?'bg-amber-100 text-amber-700':'bg-emerald-100 text-emerald-700'}`}>{isLow?'LOW':'OK'}</span></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="bg-slate-50 rounded-xl p-3"><span className="text-[10px] text-slate-400 block uppercase">Stock</span><span className="font-black text-slate-900">{st.currentStock} {st.unit}</span></div><div className="bg-slate-50 rounded-xl p-3"><span className="text-[10px] text-slate-400 block uppercase">GST</span><span className="font-bold text-blue-700">{st.gstRate}%</span></div><div className="bg-slate-50 rounded-xl p-3"><span className="text-[10px] text-slate-400 block uppercase">Buying</span><span className="font-semibold">{formatCurrency(st.purchasePrice)}</span></div><div className="bg-slate-50 rounded-xl p-3"><span className="text-[10px] text-slate-400 block uppercase">Selling</span><span className="font-semibold">{formatCurrency(st.sellingPrice)}</span></div></div><div className="flex justify-between text-xs border-t border-slate-100 pt-2"><span className="text-slate-500">Stock valuation</span><span className="font-black">{formatCurrency(st.currentStock*st.purchasePrice)}</span></div></article>})}</div></div>
    {showAddModal && <div className="fixed inset-0 z-50 bg-slate-900/60 backdrop-blur-sm flex items-center justify-center p-3 sm:p-4 overflow-y-auto"><div className="bg-white border border-slate-200 w-full max-w-lg max-h-[calc(100dvh-1.5rem)] overflow-y-auto rounded-2xl p-4 sm:p-6 shadow-2xl space-y-5 text-slate-800"><div className="flex items-center justify-between border-b border-slate-100 pb-3"><h3 className="text-base font-bold flex items-center gap-2"><Package className="w-5 h-5 text-blue-600" /> Add Inventory Item</h3><button onClick={()=>setShowAddModal(false)} className="min-h-10 min-w-10 flex items-center justify-center text-slate-400"><X className="w-5 h-5" /></button></div><div className="space-y-3 text-xs"><div><label className="font-bold block mb-1">Item / SKU Name *</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Wireless Gaming Mouse RGB" className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2 font-semibold" /></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-3"><div><label className="font-bold block mb-1">HSN Code</label><input value={hsn} onChange={e=>setHsn(e.target.value)} placeholder="84716060" className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2 font-mono" /></div><div><label className="font-bold block mb-1">Category</label><select value={category} onChange={e=>setCategory(e.target.value)} className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2"><option>Computer Peripherals</option><option>Displays</option><option>Accessories</option><option>Audio</option><option>Electronics</option></select></div></div><div className="grid grid-cols-1 sm:grid-cols-3 gap-3"><div><label className="font-bold block mb-1">Opening Qty</label><input type="number" min="0" value={currentStock} onChange={e=>setCurrentStock(Number(e.target.value))} className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2 text-center" /></div><div><label className="font-bold block mb-1">Buying Rate</label><input type="number" value={purchasePrice} onChange={e=>setPurchasePrice(Number(e.target.value))} className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2" /></div><div><label className="font-bold block mb-1">Selling Rate</label><input type="number" value={sellingPrice} onChange={e=>setSellingPrice(Number(e.target.value))} className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2" /></div></div><div><label className="font-bold block mb-1">GST Tax Rate %</label><select value={gstRate} onChange={e=>setGstRate(Number(e.target.value))} className="w-full bg-slate-50 border border-slate-300 rounded-xl px-3 py-2"><option value={18}>18% GST</option><option value={12}>12% GST</option><option value={5}>5% GST</option><option value={28}>28% GST</option></select></div></div><div className="flex flex-col-reverse sm:flex-row justify-end gap-2 pt-3 border-t border-slate-100"><button onClick={()=>setShowAddModal(false)} className="min-h-11 px-4 py-2 rounded-xl text-xs font-semibold bg-slate-100">Cancel</button><button onClick={handleSaveItem} className="min-h-11 flex items-center justify-center gap-2 text-xs font-bold px-5 py-2.5 rounded-xl bg-blue-600 text-white"><CheckCircle2 className="w-4 h-4" /> Save SKU</button></div></div></div>}
  </div>;
};